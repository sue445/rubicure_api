# https://circleci.com/docs/config-sample
machine:
  ruby:
    version: 2.4.0

dependencies:
  pre:
    - gem update --system

## Customize test commands
test:
  override:
    - bundle exec rake test

deployment:
  production:
    branch: master
    heroku:
      appname: rubicure
