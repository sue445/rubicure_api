# https://circleci.com/docs/config-sample
machine:
  ruby:
    version: 2.3.0

## Customize test commands
test:
  override:
    - bundle exec rake test

deployment:
  production:
    branch: master
    heroku:
      appname: rubicure
